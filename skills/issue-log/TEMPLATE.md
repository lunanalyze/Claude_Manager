---
title: <증상 한 줄>
date: <YYYY-MM-DD>
type: 실패→해결        # 실패→해결 | 검증
status: resolved       # resolved | workaround | verified | open
stack: <관련 스택/도구/환경>   # 예: WSL Ubuntu, Next.js 15, Python3
tags: [<검색어1>, <검색어2>, <검색어3>]   # 미래 세션이 이 이슈를 찾을 키워드
---

# <증상 한 줄>

## 언제 참고
<이 이슈가 다시 튀어나올 상황을 구체적으로. "…하려고 할 때", "…환경에서 …하면".
 미래 세션이 자기 상황과 매칭할 수 있게.>

## 무엇을 하려 했나
<사용자가 요청한 것 / 하려던 작업. 한두 줄.>

## 무슨 일이 있었나
<실패라면: 어떻게·몇 번 실패했는지 + 실제 에러/출력.
 검증이라면: 무엇이 불확실해 확인이 필요했는지.>

```text
<실제 에러 메시지 / 명령 출력 발췌 — 지어내지 말 것>
```

## 원인
<밝혀진 근본 원인. 모르면 "미상 — 정황상 …"이라고 솔직히 적는다.>

## 해결 (실제로 통한 것)
<재현 가능한 해결책. 실행한 명령/코드/설정 변경을 그대로. 출처 `파일:라인`.
 Claude가 스스로 푼 것인지, 사용자 지시/대화로 푼 것인지도 적으면 좋다.>

```bash
<실제 해결 명령 / 코드>
```

## 다음엔 이렇게 (재발 방지 규칙)
<행동 가능한 지침 — 이 문서의 핵심.
 예: "이 작업 전 `X`부터 확인하라", "`Y`는 이 환경에서 쓰지 말고 `Z`로", "실패하면 곧장 `A`를 본다".>

## 검증 방법
<해결됐는지 확인하는 법 — 명령과 기대 출력.>

## 출처
<세션 날짜 / 커밋 해시 / 관련 파일. 환경·버전 특정적이면 명시.>
