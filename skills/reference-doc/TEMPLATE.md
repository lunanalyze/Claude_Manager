# <기능/패턴 이름> — <한 줄 요약>

> 참고(reference) 문서 · 강제 표준 아님. "이런 요구를 이렇게 풀었다"는 재사용 사례.

- **프로젝트**: <어느 프로젝트/repo에서 구현했나>
- **스택/버전**: <핵심 라이브러리·프레임워크와 버전> (버전 의존적 동작이면 꼭 명시)
- **관련 파일**: `<경로:줄범위>`, `<경로:줄범위>` …
- **작성일**: <YYYY-MM-DD>
- **키워드**: <검색어들 — 미래 세션이 이 문서를 찾을 단어. 예: optimistic update, rollback, POST 병합>

## 언제 참고
<이 문서가 유용한 상황을 1–3개. "…를 구현할 때", "…문제를 만났을 때" 형태로.
 미래 세션이 자기 상황과 매칭할 수 있게 구체적으로.>

## 문제 / 목표
<무엇을 만들려 했는가, 어떤 요구·제약이 있었는가. 왜 단순 접근으로는 부족했는가.>

## 접근 방식 (핵심 아이디어)
<해법의 큰 그림을 산문으로. 데이터 흐름/역할 분담을 먼저 말로 설명한 뒤 코드로 내려간다.
 다이어그램이 도움되면 간단한 텍스트 흐름도(예: A → B → C)를 넣는다.>

## 핵심 코드
<실제 파일에서 발췌. 각 블록 위에 출처(`파일경로:줄번호`)와 한 줄 설명. 사이는 // … 로 생략.
 여러 파일이면 역할별로 나눠 제시(예: 상태 훅 / API 라우트 / 렌더).>

```<lang>
// <경로:줄번호> — <이 조각이 하는 일>
<실제 코드 발췌>
```

<코드 아래에 "이 부분이 왜 이렇게 생겼는지"를 짚는다. 특히 눈에 안 띄는 한 줄의 의도.>

## 동작 원리 / 흐름
<런타임에 무슨 일이 순서대로 일어나는지. 엣지 케이스(실패·롤백·빈 상태 등) 처리 포함.>

## 결정과 함정 (왜 이렇게 했나)
- **<결정 1>**: <택한 이유 / 버린 대안 / 트레이드오프>
- **<함정 1>**: <겪을 수 있는 문제와 회피법. 버전·환경 의존적 주의점 포함>

## 다른 프로젝트에 재사용하려면 (체크리스트)
- [ ] <이식할 때 바꿔야 하는 값·경로·의존성>
- [ ] <전제 조건(설치 패키지, 설정 파일, 토큰 등)>
- [ ] <검증 방법(어떻게 동작을 확인하나)>

## 출처
- <repo / 커밋 해시 / 파일:라인>. 원본이 바뀔 수 있으니, 재현 전 현재 코드와 대조 권장.
