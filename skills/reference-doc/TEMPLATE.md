# <기능/패턴 이름> — <한 줄 요약>

> 참고(reference) 문서 · 강제 표준 아님. "이런 요구를 이렇게 풀었고, 이렇게 검증한다"는 재사용 사례.

- **프로젝트**: <어느 repo/모듈>
- **스택/버전**: <핵심 라이브러리·프레임워크와 버전> (버전 의존 동작이면 명시)
- **관련 파일**: `<경로:줄범위>`, `<경로:줄범위>` …
- **작성일**: <YYYY-MM-DD>
- **키워드**: <검색어들 — 미래 세션이 이 문서를 찾을 단어>

## 언제 참고
<이 패턴이 유용한 상황 1–3개. "…를 구현할 때", "…를 검증해야 할 때" 형태로 구체적으로.>

## 문제 / 목표
<무엇을 만들려 했는가, 어떤 제약·요구가 있었는가. 왜 단순 접근으론 부족했는가.>

## 접근 방식 (핵심 아이디어)
<해법의 큰 그림을 산문으로. 역할 분담·데이터 흐름을 말로 먼저(예: A → B → C), 그다음 코드.>

## 핵심 코드
<실제 파일에서 발췌. 각 블록 위에 출처(`파일:줄`)와 한 줄 설명. 사이는 // … 로 생략.>

```<lang>
// <경로:줄번호> — <이 조각이 하는 일>
<실제 코드 발췌>
```

<코드 아래에 "왜 이 한 줄이 이렇게 생겼는지" 등 비자명한 의도를 짚는다.>

## 동작 원리 / 흐름
<런타임에 순서대로 무슨 일이 일어나는지. 엣지 케이스(실패·빈 상태·경계) 처리 포함.>

## 검증 방법 (How to verify)  ← 필수
<이 기능이 실제로 되는지 확인하는 법. 실행할 명령/입력과 **기대 출력**까지 구체적으로.
 "해줘" 했을 때 한 번에 됐는지 못 믿을 부분이라, 재현 가능한 검증을 남긴다.>

```bash
# 예: 라우트/가드/파서 동작 확인
<검증 명령>            # 기대: <기대 출력>
```

- 정상 신호: <무엇이 보이면 OK>
- 흔한 오판: <검증이 잘못 나오는 함정 — 예: SSR HTML엔 안 보이지만 정상 등>

## 결정과 함정 (왜 이렇게 했나)
- **<결정 1>**: <택한 이유 / 버린 대안 / 트레이드오프>
- **<함정 1>**: <겪을 수 있는 문제와 회피법. 환경·버전 주의점 포함>

## 다른 프로젝트에 재사용하려면 (체크리스트)
- [ ] <이식 시 바꿔야 할 값·경로·의존성>
- [ ] <전제 조건(설치 패키지·설정·토큰 등)>
- [ ] <검증 방법으로 동작 확인>

## 출처
- <repo / 커밋 해시 / 파일:라인>. 원본이 바뀔 수 있으니 재현 전 현재 코드와 대조 권장.
